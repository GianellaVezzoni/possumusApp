import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  footerText: {
    backgroundColor: '#D3CACA',
    height: 30,
    textAlign: 'center',
    paddingVertical: 5,
    position: 'relative'
  }
});